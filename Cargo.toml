[package]
name = "ch32h417-blink"
version = "0.1.0"
edition = "2021"

[dependencies]
panic-halt = "0.2.0"

[profile.dev]
panic = "abort"
opt-level = "s"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
codegen-units = 1
